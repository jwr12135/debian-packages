{
  "name": "debian-packages",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "update-packages": "mkdir -p debian/.tmp && node ./scripts/get-packages.js ; node ./scripts/gen-packages.js ; rm -r debian/.tmp"
  },
  "dependencies": {
    "@octokit/rest": "^18.0.9",
    "follow-redirects": "^1.13.0"
  }
}
